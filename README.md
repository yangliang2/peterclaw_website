# Peterclaw Website

Personal website scaffold built with Astro, Tailwind CSS, and TypeScript.

## Commands

```sh
pnpm install
pnpm dev
pnpm build
pnpm preview
```

The project is configured for Vercel static deployment with `pnpm build` and
`dist` as the output directory.
