---
import { getCollection } from "astro:content";
import ContentLayout from "../../layouts/ContentLayout.astro";

const projects = (await getCollection("projects")).filter((project) => !project.data.draft);
---

<ContentLayout title="Projects" description="项目作品、实验产品与案例记录。">
  {projects.length > 0 ? (
    <div class="grid gap-4 sm:grid-cols-2">
      {projects.map((project) => (
        <article class="border border-line bg-surface/60 p-5 transition duration-300 hover:border-accent/70">
          <h2 class="text-xl font-semibold text-white">
            {project.data.href ? (
              <a class="transition hover:text-accent" href={project.data.href} target="_blank" rel="noreferrer">
                {project.data.title}
              </a>
            ) : (
              project.data.title
            )}
          </h2>
          <p class="mt-2 text-sm leading-6 text-muted">{project.data.description}</p>
          {project.data.tags.length > 0 && (
            <div class="mt-4 flex flex-wrap gap-2">
              {project.data.tags.map((tag) => (
                <span class="border border-accent-soft bg-accent-soft/20 px-2 py-1 font-mono text-xs text-accent">{tag}</span>
              ))}
            </div>
          )}
        </article>
      ))}
    </div>
  ) : (
    <p class="max-w-2xl text-base leading-8 text-muted">
      Project entries will be added as the portfolio content is finalized.
    </p>
  )}
</ContentLayout>
